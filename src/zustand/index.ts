// import type { UserSlice } from './slices.types'

// import { create } from 'zustand'

// // import { createUserSlice } from './user'

// export const useBoundStore = create<UserSlice>((...a) => ({
//   ...createUserSlice(...a)
// }))
