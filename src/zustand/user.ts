// import type { SliceCreator, UserSlice } from './slices.types'

// export const createUserSlice: SliceCreator<UserSlice> = set => ({
//   user: null,
//   addFish: () => set(state => ({ fishes: state.fishes + 1 }))
// })
