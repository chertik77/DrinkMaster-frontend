'use client'

export const Header = () => {
  // const { data } = useQuery({
  //   queryKey: ['profile'],
  //   queryFn: () => axiosInstance.get('/user/profile')
  // })

  return <div>hello</div>
}
